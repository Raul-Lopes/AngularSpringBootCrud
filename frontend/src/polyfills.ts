/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * See https://angular.io/guide/browser-support
 */

/***************************************************************************************************
 * BROWSER POLYFILLS
 */

/** IE10 and IE11 requires the following for NgClass support on SVG elements */
// import 'classlist.js';  // Run `npm install --save classlist.js`.

/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 *
 * Import zone.js using the package name. Do NOT import using a path like
 * 'node_modules/zone.js' — that is incorrect. Use 'zone.js' (or 'zone.js/dist/zone')
 * which resolves from node_modules properly.
 */
import 'zone.js'; // Included with Angular CLI.

/***************************************************************************************************
 * APPLICATION IMPORTS
 */
